# Email Configuration Setup

## Primary Provider
- **Current:** Gmail (via IMAP/SMTP or OAuth/App Password)
- **Extensibility:** Designed with a flexible provider interface so switching to Outlook, Office 365, or custom IMAP/SMTP later requires minimal configuration updates.

## Connection Details Required (Gmail)
- **Account Email:** (To be configured)
- **Auth Method:** App Password or OAuth2 Token
- **IMAP Host:** `imap.gmail.com` (Port 993)
- **SMTP Host:** `smtp.gmail.com` (Port 465 / 587)
